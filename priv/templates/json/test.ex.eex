defmodule ExAws.<%= @module %>.Test do
  use ExUnit.Case, async: true

  test "Basic integration test" do
    assert {:ok, _} = ExAws.<%= @module %>.new
    |> ExAws.<%= @module %>.Core.<%= @test_op || raise "missing test_op for #{@module}" %>(%{})
  end
end
